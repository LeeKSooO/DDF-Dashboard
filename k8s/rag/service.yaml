apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: ddf-system
  labels:
    app: rag
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: rag